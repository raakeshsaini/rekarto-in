---
const { hasPartner } = Astro.props;
---

{hasPartner ? (
  <span class="inline-flex items-center gap-2 text-xs font-medium px-3 py-1 rounded-full bg-zinc-100 text-zinc-700 border border-zinc-300">
    Partner Assigned
  </span>
) : (
  <span class="inline-flex items-center gap-2 text-xs font-medium px-3 py-1 rounded-full bg-green-50 text-green-700 border border-green-200">
    Partner Available
  </span>
)}
